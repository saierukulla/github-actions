name: my first workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps: 
        - name: welcome page
          run: "my first github actions porject"

        - name: List files
          run: ls

        - name: readme file
          run: cat README.md
